// Defined at build time from `name` in package.json — see `define` in
// vite.config.ts. Read them through `config/game.name.ts`, not directly.
declare const __GAME_NAME__: string;
declare const __GAME_TITLE__: string;
